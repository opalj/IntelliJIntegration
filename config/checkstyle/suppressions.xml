<?xml version="1.0"?>

<!DOCTYPE suppressions PUBLIC
        "-//Puppy Crawl//DTD Suppressions 1.1//EN"
        "http://www.puppycrawl.com/dtds/suppressions_1_1.dtd">

<suppressions>
    <!-- contains some files that need to be checked (e.g. JavaByteCodePsiImplUtil.java), for now is ok -->
    <suppress files="JavaByteCode\w+\.java" checks="[a-zA-Z0-9]*"/>

    <suppress files="TAC.*\.java" checks="[a-zA-Z0-9]*"/>
</suppressions>